@model TrashCollector.Models.Customer

@{
    ViewData["Title"] = "RequestOneTimePickUp";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>RequestOneTimePickUp</h1>

<h4>Customer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="RequestOneTimePickUp">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Account.OneTimePickup" class="control-label"></label>
                <input asp-for="Account.OneTimePickup" class="form-control" type ="date" />
                <span asp-validation-for="Account.OneTimePickup" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
